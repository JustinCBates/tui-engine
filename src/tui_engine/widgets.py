raise ImportError(
    "Top-level module 'tui_engine.widgets' was removed. Import domain factories using:\n"
    "    import tui_engine.factories as widgets\n"
    "and use the adapter package 'tui_engine.widgets' for backend widget implementations."
)
